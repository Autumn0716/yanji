---
title: 代码解释
description: 1. 这里timesinceepoch是计算距离Unix纪元(1970-01-01 00:00:00)的时间长度 chrono是指标准时间库 unsigned seed = chrono::systemclock::now().timesinceepoch().count();
tags:
- imported-from-obsidian
---

1. 这里`time_since_epoch`是计算距离Unix纪元(1970-01-01 00:00:00)的时间长度
chrono是指标准时间库
`unsigned seed = chrono::system_clock::now().time_since_epoch().count();
2. 打乱数据
`shuffle(nums.begin(),nums.end(),default_random_engine(seed)`
